"""User-facing interfaces over the core classifier.

Each subpackage is a separate entry point with its own console script:

- ``cli``    — ``zero-shot``
- ``server`` — ``zero-shot-serve``

Interfaces only import from :mod:`zero_shot.core`.
"""
